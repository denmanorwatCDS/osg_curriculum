{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make-logs-dir",
            "type": "shell",
            "command": "mkdir -p logs",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            }
        }
    ]
}
